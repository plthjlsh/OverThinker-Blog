---
---

<h1>Environment Info</h1>
<p>import.meta.env.MODE: {import.meta.env.MODE}</p>
<p>import.meta.env.DEV: {String(import.meta.env.DEV)}</p>
<p>import.meta.env.PROD: {String(import.meta.env.PROD)}</p>